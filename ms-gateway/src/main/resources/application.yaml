server:
  port: 8085

spring:
  application:
    name: ms-gateway

  data:
    redis:
      host: redis
      port: 6379

# Feign timeout-lar
feign:
  client:
    config:
      default:
        connectTimeout: 3000
        readTimeout: 3000
